import EditProfile from "./page-content";

export function generateMetadata() {
  return {
    title: "Edit Profile",
  };
}

const UpdateProfile = () => {
  return <EditProfile />;
};

export default UpdateProfile;
